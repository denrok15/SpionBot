[tool.ruff]
line-length = 88  # Максимальная длина строки

[tool.ruff.lint]
select = ["E", "F", "I"]  # правила Flake8 (E, F) и сортировка импортов (I)
ignore = ["E722","F405","E501"]    # Игнорировать конкретные ошибки